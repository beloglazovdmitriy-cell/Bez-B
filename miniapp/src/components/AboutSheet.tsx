import { Brand } from "./Icons";

// «О проекте» — манифест портфеля Без Б. Статичный текст (синхронизирован с MANIFEST.md).
const MANIFEST: { h: string; body: string[] }[] = [
  {
    h: "Кто я",
    body: [
      "Обычный человек, который начал с нуля. У меня есть 10 000 ₽ раз в 2 недели на инвестиции — как у статистического человека, а не у фонда.",
      "Я показываю не «как разбогатеть», а как дисциплина и система работают на длинной дистанции. Без обещаний. Без задним числом. Без буллшита.",
    ],
  },
  {
    h: "⏱ Ритм взносов",
    body: [
      "10 000 ₽ каждые 2 недели. Всегда. Рынок падает — взнос идёт. Растёт — взнос идёт. Страшно — взнос идёт.",
      "Я не угадываю дно и не жду удобного момента. Удобный момент — это сегодня, по графику. Пропущенный взнос ломает систему сильнее, чем неудачная сделка.",
    ],
  },
  {
    h: "⚖️ Диверсификация без жёстких процентов",
    body: [
      "Распределяю между несколькими классами: крипта, акции США, золото. Фиксированных долей нет — пропорции меняются по мере роста капитала и обстановки на рынке.",
      "Растущее не продаю ради ровных процентов — направляю свежие взносы туда, где вижу смысл.",
    ],
  },
  {
    h: "✅ Что я делаю",
    body: [
      "Покупаю строго по плану. На каждую сделку записываю причину — она видна в Журнале. Держу несколько классов, структуру меняю по мере роста капитала. Думаю горизонтом в годы.",
    ],
  },
  {
    h: "❌ Что я НЕ делаю",
    body: [
      "Без плеча и шортов. Не выхожу в кэш на эмоциях. Не ловлю дно и не угадываю верх. Не гоняюсь за хайпом без причины. Не рисую доходность задним числом и не прячу минусы.",
    ],
  },
  {
    h: "🔍 Честность — часть системы",
    body: [
      "Когда портфель в минусе — я говорю об этом и объясняю, почему не дёргаюсь. Цена закрытия пишется вперёд каждый день → трек-рекорд проверяемый, его нельзя переписать. Журнал сделок открыт.",
    ],
  },
];

export default function AboutSheet({ onClose }: { onClose: () => void }) {
  return (
    <div className="sheet-overlay" onClick={onClose}>
      <div className="sheet" onClick={(e) => e.stopPropagation()}>
        <div className="sheet-grip" />
        <div className="sheet-title">О проекте · Манифест <Brand size={16} /></div>

        <div className="ai-text">
          {MANIFEST.map((s, i) => (
            <div key={i} style={{ marginBottom: 14 }}>
              <div style={{ fontWeight: 700, marginBottom: 4 }}>{s.h}</div>
              {s.body.map((p, j) => (
                <p key={j} style={{ margin: "0 0 6px" }}>{p}</p>
              ))}
            </div>
          ))}
          <div className="disclaimer" style={{ marginTop: 4 }}>
            Это публичный эксперимент и аналитика, а не сигналы и не инвестиционная
            рекомендация. Решения о деньгах — твои. Прошлые результаты не гарантируют будущих.
          </div>
        </div>

        <button className="sheet-cancel" onClick={onClose}>Закрыть</button>
      </div>
    </div>
  );
}
